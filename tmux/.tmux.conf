# Keep one source of truth.
#
# tmux reads ~/.tmux.conf by default; source the XDG config so you don't end up
# editing ~/.config/tmux/tmux.conf while tmux is actually using ~/.tmux.conf.
source-file ~/.config/tmux/tmux.conf

