[include]
    path = config_public
[include]
    path = config_private
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[credential]
	helper =
	helper = "!exec \"${XDG_CONFIG_HOME:-$HOME/.config}/git/credential-helper\""
